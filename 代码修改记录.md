# 代码修改记录

## 2026-06-11 16:41:19 +08:00
- 修改内容：在 `streamlit_app.py` 中新增 `_ensure_column_at_end()`，并在读取电子元器件工作表时自动补齐 `立创编号` 字段，同时固定为最后一列。
- 涉及文件：`streamlit_app.py`、`.gitignore`、`代码修改记录.md`
- 修改原因或目标：满足电子元器件管理表最后一列增加立创编号的需求，方便录入和查看 LCSC 编号；同时补充基础 `.gitignore`，避免虚拟环境、缓存、构建产物和大文件误提交。
- 验证结果或后续注意事项：已完成代码层修改；后续首次保存电子元器件表时，云端工作表会随保存写入 `立创编号` 列。

## 2026-06-17 15:29:30 +08:00
- 修改内容：调整 `streamlit_app.py` 的排序逻辑，新增默认智能排序列配置，使 `参数` 列在主排序或次排序中都会按单位换算后的数值排序；勾选“智能排序”后，主排序列和次排序列都会启用数值单位排序。
- 涉及文件：`streamlit_app.py`、`代码修改记录.md`
- 修改原因或目标：修复按参数排序时 `KΩ`、`MΩ` 等带单位参数按字符串顺序排列的问题，确保不带 K 的欧姆值小于 KΩ，MΩ 排在更大的位置。
- 验证结果或后续注意事项：已通过 `python -m py_compile streamlit_app.py` 语法检查；已用样本验证排序结果为 `0R -> 0.5R -> 20R -> 499R -> 1K -> 1.2K -> 4.7K -> 5.1K -> 10K -> 40.2K -> 124K -> 1M`。若后续需要区分小写 `m` 表示毫单位或兆单位，可再按具体录入规范细化规则。
